import 'package:flutter/material.dart';

class LoginPetmiliyView extends StatelessWidget {
  const LoginPetmiliyView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("첫 번째 페이지"),
      ),
    );
  }
}
